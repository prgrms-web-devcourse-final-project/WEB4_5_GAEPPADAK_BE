{
  "name": "@kkokkio/infra",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "doppler": "doppler secrets download --no-file --format env-no-quotes > .env && echo '테라폼 용 .env 파일을 생성했습니다.'"
  }
}
