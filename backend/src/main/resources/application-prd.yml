spring:
  jpa:
    hibernate:
      ddl-auto: create # Entity ↔ DB 스키마 일치 여부만 검사, 실제 반영은 안 함

  flyway:
    enabled: false
    validateOnMigrate: false
    cleanDisabled: true

logging:
  level:
    root: WARN
    org.hibernate: WARN

mock:
  enable: false